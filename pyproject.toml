[project]
name = "antfarm-test"
version = "0.1.0"
dependencies = [
  "fastapi==0.115.0",
  "uvicorn==0.30.0",
  "pytest==8.3.0",
  "httpx==0.27.0",
]

[tool.pytest.ini_options]
testpaths = ["."]

[tool.mypy]
strict = false
python_version = "3.12"
